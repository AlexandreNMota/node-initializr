<% if (isTypeScript) { %>
    import { app } from './app.js';
    
    const port = Number(process.env.PORT ?? 3000);
    
    app.listen(port, () => {
      console.warn(`API running on port ${port}`);
    });
    <% } %>