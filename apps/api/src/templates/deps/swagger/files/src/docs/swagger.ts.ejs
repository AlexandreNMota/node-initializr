<% if (isTypeScript) { %>
    export const swaggerDocument = {
      openapi: '3.0.0',
      info: {
        title: '<%= name %>',
        version: '1.0.0',
      },
      paths: {
        '/api/health': {
          get: {
            summary: 'Health check',
            responses: {
              200: {
                description: 'API is healthy',
              },
            },
          },
        },
      },
    };
    <% } %>