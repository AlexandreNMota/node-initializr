<% if (isTypeScript) { %>
    export default {
      testEnvironment: 'node',
      transform: {
        '^.+\\.(t|j)s$': ['ts-jest', { tsconfig: './tsconfig.json' }],
      },
      testMatch: ['**/*.test.ts'],
    };
    <% } %>